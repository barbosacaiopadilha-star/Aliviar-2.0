import { STAGE_MS } from "./stage-tokens";

/** Momento em que o visitante pode tocar a luz — após a primeira frase se assentar. */
export const THRESHOLD_GESTURE_READY_MS = STAGE_MS.gestureReady;

/** Instrução editorial mínima quando o gesto está disponível. */
export const THRESHOLD_GESTURE_HINT = "Toque a luz.";
