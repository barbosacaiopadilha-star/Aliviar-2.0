/** Experiência narrativa de evolução da jornada — não é rastreador nem timeline técnica. */
export interface CuradoriaComecouView {
  headline: string;
  narrative: string;
  continuation: string;
  patientName: string;
  journeyState: string;
  portalHref: string;
  startedAt: string;
}
